const List<String> batchList = ['BCS', 'IMT', 'IMG'];
const List<String> yearJoinedList = ['2019', '2020', '2021', '2022', '2023'];

enum AdminApprovalStatus { pending, accepted, denied }

enum ValidationStatus { accepted, pending, denied, nullStatus }

enum AppProgressStatus { registered, emailVerified, loggedIn }
